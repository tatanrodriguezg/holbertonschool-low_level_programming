#include <stdio.h>
/**
 * main - function description
 * Description: used to print
 * Return: zero is returned
 */
int main(void)
{
	int x;

	x = 0;

	while (x < 9)
	{
		printf("%d", x);
		x++;
		}
	printf("%d\n", x);
	return (0);
}
